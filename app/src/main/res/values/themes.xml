<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppAddRemoveItems" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->


        <!-- Asociamos el estilo creado al objeto que representa los tres botones -->
        <item name="actionOverflowButtonStyle">@style/TresPuntosColor</item>
    </style>

    <style name="Theme.AppAddRemoveItems" parent="Base.Theme.AppAddRemoveItems" />

    <!-- Definimos el estilo de los botones -->
    <style name="TresPuntosColor" parent="Widget.AppCompat.ActionButton.Overflow">

        <item name="tint">#ffffff</item>

    </style>
</resources>